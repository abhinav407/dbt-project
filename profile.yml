default:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: sab-dev-proj-dev-dw-4905  
      dataset: DBT  
      threads: 4
      keyfile: /secrets/dbt-secret
      timeout_seconds: 300
      priority: interactive
      retries: 1